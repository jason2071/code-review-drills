// Drill registry. Each category lives in its own file under data/ and
// appends itself via DATA.push(...). Load order: this file first, then the
// data/*.js category files (in numeric order), then app.js.
const DATA = [];
